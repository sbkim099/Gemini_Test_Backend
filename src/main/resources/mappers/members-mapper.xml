<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Members">
  <select id="dupCheck" resultType="int">
  	select count(*) from members where id=#{id}
  </select>
  <select id="isLogin" resultType="int">
  	select count(*) from members where id=#{id} and pw=#{pw} 
  </select>
  <select id="selectOneMember" resultType="com.study.app.domains.members.MembersDTO">
  	select * from members where id=#{id}
  </select>
  <insert id="signUp">
  	insert into members values(#{id},#{pw},#{name},#{phone},#{email},#{zipcode},#{address1},#{address2},sysdate)
  </insert>
  <delete id="delMember">
  	delete members where id=#{id}
  </delete>
  <update id="upMember">
  	update members set name=#{dto.name}, email=#{dto.email}, phone=#{dto.phone},
  	zipcode=#{dto.zipcode},address1=#{dto.address1},address2=#{dto.address2} 
  	where id=#{id}
  </update>
</mapper>