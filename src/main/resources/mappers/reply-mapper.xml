<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Reply">
  	
  	<select id="selectByParentSeq" resultType="com.study.app.domains.reply.ReplyDTO">
  		select * from reply where parent_seq = #{parent_seq}
  	</select>
  	
  	<insert id="insert">
  		insert into reply values(reply_seq.nextval, #{writer}, #{contents}, sysdate, #{parent_seq})
  	</insert>
  	
  	<update id="update">
  		update reply set contents = #{contents} where seq = #{seq}
  	</update>
  	
  	<delete id="delete">
  		delete from reply where seq = #{seq}
  	</delete>
</mapper>